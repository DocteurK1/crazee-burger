import React from "react";

export default function HandleAdminDisplay({ isModeAdmin }) {
  // D’abord on définit les states de base (état, données, variable…)

  // // Comportements, les actions, la logique

  const handleAdminDisplay = () => {
    // console.log("Is Mode Admin : ", isModeAdmin);
    // // Handle the display of the admin panel
    // if (isModeAdmin) {
    //   console.log("Display admin");
    // } else {
    //   console.log("Hide admin");
    // }
  };

  handleAdminDisplay();

  // L’affichage, le render, via return

  return <div>HandleAdminDisplay</div>;
}
